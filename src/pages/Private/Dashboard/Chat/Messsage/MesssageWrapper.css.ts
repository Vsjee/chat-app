import styled from 'styled-components';

const MesssageWrapper = styled.li`
  margin-right: 5%;
  background-color: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(20px);
  padding: 2px 20px;
  border-radius: 15px;
  border-bottom-right-radius: 0;
  font-size: 1rem;
`;

export default MesssageWrapper;
